import 'package:flutter/material.dart';

class SmallLineWidgetDesktop extends StatelessWidget {
  const SmallLineWidgetDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1,
      width: 734,
      color: const Color.fromRGBO(22, 26, 30, 1),
    );
  }
}
